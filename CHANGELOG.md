# سجلّ التغييرات — Changelog

تُوثَّق هنا التغييرات البارزة في **مُحسِّن الموجّهات (Muharrir)**. الترتيب من الأحدث إلى الأقدم.

## غير مُصدَر (Unreleased)

### جاهزية المصدر المفتوح والمنح
- تحديث هوية الحزمة إلى `muharrir` مع رخصة MIT وبيانات repository وkeywords.
- إضافة وثائق الحوكمة: `CONTRIBUTING.md` و`SECURITY.md` و`CODE_OF_CONDUCT.md` وقوالب issues/PR.
- إعادة كتابة README حول التموضع المحلي أولًا، Demo Mode، الخصوصية، المعمارية، والمساهمة.
- إضافة وثائق الخصوصية، خارطة الطريق، مزوّدي OpenAI-compatible، وسجل قرارات جاهزية OSS.
- تحسين CI ليشغّل `npm ci` وlint وtypecheck واختبارات الوحدة والبناء وPlaywright smoke.
- تحسين Demo Mode بتدفق محلي واقعي وأدوات `suggest_enhancements` و`propose_prompt`.
- إضافة تصدير للموجّه المقترح، واختبارات Playwright للنسخ/الحفظ/التصدير ورفع TXT/PDF/DOCX.
- إضافة اختبارات وحدة للمزوّدين، استيراد/تصدير المفضّلة، وتحليل الملفات النصية.
- منع fallback حفظ مفتاح API في `localStorage` داخل Tauri عند فشل OS Keychain.

## 0.2.3 — 2026-06-21

### القوالب
- **توسعة قوالب الأوامر من 4 إلى 19 قالبًا** مصنّفًا (كتابة، أعمال/تسويق، برمجة/بيانات، تعليم/بحث)، **ثنائية اللغة** (ar/en)، وكل قالب يبذر نصّ بداية غنيًّا (دور + سياق + مهمّة + صيغة مخرجات) بدل الجملة الرفيعة السابقة.

## 0.2.2 — 2026-06-21

### الواجهة
- **أيقونة تطبيق جديدة**: مربّع دافئ بزوايا دائرية + فقاعة موجّه وبريق، بدلًا من البانر العريض المضغوط القديم غير المقروء في الأحجام الصغيرة. أُعيد توليد كل الأحجام (سطح المكتب + PWA).

## 0.2.1 — 2026-06-20

### إصلاحات
- **معالجة أوضح لأخطاء المزوّد:** التقاط **402 (رصيد غير كافٍ)** برسالة قصيرة واضحة، واستخلاص `error.message` من جسم المزوّد (JSON) واقتصار طوله عند 300 محرف — بدل تسرّب جسم الخطأ الخام الطويل إلى المحادثة (كما يفعل OpenRouter).
- تصنيف 402/«insufficient credits» كـ`quota` بدل `unknown`.
- **تطبيع مفتاح API** (قصّ المسافات/الأسطر) قبل الإرسال — سبب شائع لأخطاء 401 الغامضة.

## 0.2.0 — 2026-06-20

دفعة تحسينات شاملة على الواجهة والموثوقية وجودة الشيفرة والتدويل (PRs #7–#15).

### الواجهة والتصميم
- **إعادة تصميم الواجهة** بثيم `tweakcn` الدافئ، خطّ Rubik، وأيقونات Phosphor Duotone. (#7)
- **معرض الأنماط الجاهزة** (Preset Gallery) لبدء الموجّهات من قوالب مُعدّة. (#9)
- **مقارنة الموجّهات** (Compare Dialog) بعرض فرق نصّي بين نسختين محفوظتين. (#9)
- **بحث Spotlight** (`Ctrl+K`) للتنقّل والبحث السريع في المحادثات. (#9)
- **شارة الإصدار/التحديث**، مبدّل اللغة، ودليل اختصارات لوحة المفاتيح. (#7–#9)
- تهيئة **PWA** قابلة للتثبيت (`manifest.webmanifest` + لون الثيم) وتحسينات إمكانية الوصول (`aria-*`). (#9)

### الموثوقية والأداء
- **إصلاح حفظ مفاتيح API على سطح المكتب** (Tauri/Keychain). (#8)
- تحسينات الموثوقية وجودة الشيفرة، وتسجيل موحّد عبر `logger.ts`. (#8)
- **استخراج استهلاك بثّ المحادثة** إلى `src/lib/chat-stream.ts` (`consumeChatStream` و`classifyChatError`) بعيدًا عن المكوّن الرئيسي. (#8)
- **تذكير `MessageItem`** (`memo`) بحيث لا يُعاد تصيير سوى الرسالة النشطة أثناء البثّ — مكسب أداء في المحادثات الطويلة. (#10)

### التدويل (i18n)
- توطين **رسائل الإشعارات** العربية المضمّنة عبر مساحة `toasts`. (#11)
- توطين **بطاقة الموجّه المقترَح** عبر مساحة `promptProposal`. (#12)
- توطين **النصوص المتبقّية** (شارة الإصدار، وصف السمة، تلميح توسيع حقل الإدخال). (#13)
- النتيجة: لا نصوص واجهة ظاهرة ثابتة بالعربية لا تتبدّل مع اللغة (عدا اسم المؤلّف/حقوق النشر والنصوص الموجَّهة للنموذج).

### الأنواع والجودة
- **تشديد أنواع TypeScript** في مسار الرسائل: إزالة `any` وتطبيق `UiMessage`/`ToolInvocation` في `page.tsx` و`message-item.tsx`. (#14)
- اختبارات وحدة (**Vitest**) في `src/lib/__tests__/` تغطّي `format-validator` و`chat-stream` و`token-estimate` و`text-diff`.

### الأمان والاعتماديات
- **ترقية Next.js إلى 14.2.35** (و`eslint-config-next` معه) لإغلاق ثغرات أمنية حرجة قابلة للإصلاح في فرع 14.2 (تجاوز صلاحيات الـmiddleware، حقن المحتوى، تسميم الكاش عبر RSC) — دون تغيير في كود التطبيق وبإبقاء React 18.

### المزوّدون والإعدادات (قبل هذه الدفعة)
- كتالوج 15 مزوّدًا متوافقًا مع OpenAI + منتقي مزوّد + إضافة مزوّد يدويًّا.
- تبويب «حول» في الإعدادات (حقوق النشر + البريد + الموقع).
